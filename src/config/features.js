// Canonical UI feature flags.
// Keep these frontend-only and safe to toggle for temporary UI gating.

export const AI_PING_ENABLED = false;

// Phase 0 — UI governance flags (frontend-only)
export const FEATURE_MISSION_CONTROL_V2 = false;
export const FEATURE_AI_INVOCATION = false;
export const FEATURE_PROGRESSIVE_SURFACING = false;
export const FEATURE_GAMIFICATION_B2B = false;

// Phase 0 — guardrails
export const FEATURE_STRICT_TONE_RULES = true;
export const FEATURE_STRICT_MOTION_RULES = true;
